# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
webcrawler

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# macOS specific
.DS_Store

# Runtime persistence data (generated at runtime, not source code)
crawl_data/
